<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= planet.name %></title>
</head>
<body>
    <a href="/">Homepage</a>
    <a href="/planets/new">Add New Planet</a>
    <a href="/planets/">Browse our Solar System</a>
    <h1><%= planet.name %>:</h1>
    <% if(planet.hasRing) { %>
        <li>has at least one ring.</li>
    <% } else { %>
        <li>does not have any rings.</li>
    <% } %>

    <% if(planet.hasMoon) { %>
        <li>has at least one moon.</li>
    <% } else { %>
        <li>does not have any moons.</li>
    <% } %>

    <% if(planet.innerPlanet) { %>
        <li>is considered an inner planet.</li>
    <% } else { %>
        <li>is not considered an inner planet.</li>
    <% } %>

    <% if(planet.outerPlanet) { %>
        <li>is considered an outer planet.</li>
    <% } else { %>
        <li>is not considered an outer planet.</li>
    <% } %>
    <form action="/planets/<%= planet._id%>?_method=DELETE" method="POST"> <!-- the ?_method=DELETE is what Express will pick up for us and map the action to our app.delete route-->
        <button type="submit">Delete <%= planet.name %></button>
    </form>
    <a href="/planets/<%= planet._id %>/edit">Edit <%= planet.name %>/edit</a>
</body>
</html>